<Window x:Class="UniDesk.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:UniDesk.Helpers"
        Title="{DynamicResource Settings.Title}"
        SizeToContent="Manual"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Closed="SettingsWindow_OnClosed"
        KeyDown="SettingsWindow_OnKeyDown"
        PreviewMouseLeftButtonDown="Window_OnPreviewMouseLeftButtonDown"
        PreviewMouseMove="Window_OnPreviewMouseMove"
        PreviewMouseLeftButtonUp="Window_OnPreviewMouseLeftButtonUp"
        MouseLeave="Window_OnMouseLeave">

    <Window.Resources>
        <helpers:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <helpers:StringEqualsConverter x:Key="StringEqualsConverter"/>
        <SolidColorBrush x:Key="DlgBackground" Color="#E6FFFFFF"/>
        <SolidColorBrush x:Key="DlgTextPrimary" Color="#1E293B"/>
        <SolidColorBrush x:Key="DlgTextSecondary" Color="#64748B"/>
        <SolidColorBrush x:Key="DlgBorder" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="DlgInputBackground" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="DlgAccent" Color="#3B82F6"/>
        <SolidColorBrush x:Key="DlgAccentSoft" Color="#1A3B82F6"/>

        <Style x:Key="DlgLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource DlgTextSecondary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DlgSectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource DlgTextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="DlgChipButton" TargetType="Button">
            <Setter Property="Height" Value="26"/>
            <Setter Property="MinWidth" Value="40"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource DlgTextSecondary}"/>
            <Setter Property="Background" Value="{StaticResource DlgInputBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DlgBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource DlgAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DlgChipButtonActive" TargetType="Button" BasedOn="{StaticResource DlgChipButton}">
            <Setter Property="Foreground" Value="{StaticResource DlgAccent}"/>
            <Setter Property="Background" Value="{StaticResource DlgAccentSoft}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DlgAccent}"/>
        </Style>

        <Style x:Key="DlgGhostButton" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Background" Value="{StaticResource DlgInputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource DlgTextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DlgBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DlgPrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="{StaticResource DlgAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DlgInlineTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource DlgTextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource DlgInputBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DlgBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource DlgTextPrimary}"/>
        </Style>

        <Style x:Key="DlgSlider" TargetType="Slider">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="IsSnapToTickEnabled" Value="True"/>
        </Style>

        <Style x:Key="DlgToggleRow" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Border x:Name="DialogRoot"
            Background="{StaticResource DlgBackground}"
            BorderBrush="{StaticResource DlgBorder}"
            BorderThickness="1"
            CornerRadius="12"
            Margin="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="TitleDragArea"
                    Grid.Row="0"
                    Padding="14,10,12,6"
                    Background="Transparent"
                    Cursor="SizeAll">
                <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            IsHitTestVisible="False">
                    <Border Width="22" Height="22" CornerRadius="6" Background="{StaticResource DlgAccent}" Margin="0,0,8,0">
                        <TextBlock Text="&#xE713;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="11"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{DynamicResource Settings.Title}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource DlgTextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="CloseButton"
                        Grid.Column="1"
                        Width="26" Height="26"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CloseButton_OnClick">
                    <TextBlock Text="&#xE711;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="11"
                               Foreground="{StaticResource DlgTextSecondary}"/>
                </Button>
                </Grid>
            </Border>

            <ScrollViewer x:Name="ContentScrollViewer"
                          Grid.Row="1"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="14,0,14,4"
                          PreviewMouseLeftButtonDown="ContentScrollViewer_OnPreviewMouseLeftButtonDown"
                          PreviewMouseMove="ContentScrollViewer_OnPreviewMouseMove"
                          PreviewMouseLeftButtonUp="ContentScrollViewer_OnPreviewMouseLeftButtonUp"
                          MouseLeave="ContentScrollViewer_OnMouseLeave">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Settings.General}" Style="{StaticResource DlgSectionTitle}"/>

                    <TextBlock Text="{DynamicResource Settings.Theme}"
                               Style="{StaticResource DlgLabel}"
                               Margin="0,0,0,6"
                               VerticalAlignment="Top"/>

                    <ItemsControl x:Name="ColorSchemePanel"
                                  ItemsSource="{Binding ColorSchemes}"
                                  Margin="0,0,0,10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="30" Height="30"
                                        Margin="0,0,8,8"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        ToolTip="{Binding DisplayName}"
                                        ToolTipService.InitialShowDelay="200"
                                        ToolTipService.ShowDuration="4000"
                                        Command="{Binding DataContext.SelectColorSchemeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding Id}"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid Width="30" Height="30">
                                                <Ellipse Width="24" Height="24"
                                                         Fill="{Binding SwatchBrush}"
                                                         Stroke="{StaticResource DlgBorder}"
                                                         StrokeThickness="1"/>
                                                <Ellipse Width="28" Height="28"
                                                         Stroke="{StaticResource DlgAccent}"
                                                         StrokeThickness="2"
                                                         Visibility="Collapsed"
                                                         x:Name="SelectedRing"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter TargetName="SelectedRing" Property="Visibility" Value="Visible"/>
                                                    <Setter TargetName="SelectedRing" Property="Opacity" Value="1"/>
                                                </DataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsSelected}" Value="False"/>
                                                        <Condition Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsMouseOver}" Value="True"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter TargetName="SelectedRing" Property="Visibility" Value="Visible"/>
                                                    <Setter TargetName="SelectedRing" Property="Opacity" Value="0.55"/>
                                                </MultiDataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{DynamicResource Settings.DisplayTitle}"
                               Style="{StaticResource DlgLabel}"
                               Margin="0,0,0,6"
                               VerticalAlignment="Top"/>
                    <TextBox x:Name="DisplayTitleTextBox"
                             Style="{StaticResource DlgInlineTextBox}"
                             Text="{Binding DisplayTitle, UpdateSourceTrigger=PropertyChanged}"
                             MaxLength="20"
                             Margin="0,0,0,10"
                             PreviewMouseLeftButtonDown="EditableTextBox_OnPreviewMouseLeftButtonDown"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="44"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Settings.WindowOpacity}" Style="{StaticResource DlgLabel}" Margin="0"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding WindowOpacity, StringFormat={}{0:P0}}"
                                   FontSize="11"
                                   Foreground="{StaticResource DlgTextSecondary}"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Style="{StaticResource DlgSlider}"
                            Minimum="0.6" Maximum="1.0"
                            Value="{Binding WindowOpacity, Mode=TwoWay}"
                            TickFrequency="0.05"/>

                    <Grid Margin="0,10,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="52"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Settings.PanelWidth}" Style="{StaticResource DlgLabel}" Margin="0"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding PanelWidth, StringFormat={}{0:F0}px}"
                                   FontSize="11"
                                   Foreground="{StaticResource DlgTextSecondary}"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Style="{StaticResource DlgSlider}"
                            Minimum="320" Maximum="520"
                            Value="{Binding PanelWidth, Mode=TwoWay}"
                            TickFrequency="20"/>

                    <Grid Margin="0,10,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="52"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Settings.PanelHeight}" Style="{StaticResource DlgLabel}" Margin="0"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding PanelHeight, StringFormat={}{0:F0}px}"
                                   FontSize="11"
                                   Foreground="{StaticResource DlgTextSecondary}"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Style="{StaticResource DlgSlider}"
                            Minimum="560" Maximum="1040"
                            Value="{Binding PanelHeight, Mode=TwoWay}"
                            TickFrequency="20"/>

                    <Grid Margin="0,10,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="52"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Settings.FontSize}" Style="{StaticResource DlgLabel}" Margin="0"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding FontScaleLabel}"
                                   FontSize="11"
                                   Foreground="{StaticResource DlgTextSecondary}"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Style="{StaticResource DlgSlider}"
                            Minimum="0.9" Maximum="1.15"
                            Value="{Binding FontScale, Mode=TwoWay}"
                            TickFrequency="0.125"/>

                    <TextBlock Text="{DynamicResource Settings.ShortcutCount}"
                               Style="{StaticResource DlgLabel}"
                               Margin="0,10,0,6"
                               VerticalAlignment="Top"/>
                    <WrapPanel x:Name="ShortcutLimitPanel" Margin="0,0,0,14">
                        <Button Style="{StaticResource DlgChipButton}" Content="6" Tag="6"
                                Command="{Binding SelectShortcutLimitCommand}" CommandParameter="6"/>
                        <Button Style="{StaticResource DlgChipButton}" Content="9" Tag="9"
                                Command="{Binding SelectShortcutLimitCommand}" CommandParameter="9"/>
                        <Button Style="{StaticResource DlgChipButton}" Content="12" Tag="12"
                                Command="{Binding SelectShortcutLimitCommand}" CommandParameter="12"/>
                        <Button Style="{StaticResource DlgChipButton}" Content="15" Tag="15"
                                Command="{Binding SelectShortcutLimitCommand}" CommandParameter="15"/>
                        <Button Style="{StaticResource DlgChipButton}" Content="18" Tag="18"
                                Command="{Binding SelectShortcutLimitCommand}" CommandParameter="18"/>
                    </WrapPanel>

                    <TextBlock Text="{DynamicResource Settings.Language}"
                               Style="{StaticResource DlgLabel}"
                               Margin="0,0,0,6"
                               VerticalAlignment="Top"/>
                    <ItemsControl ItemsSource="{Binding LanguageOptions}" Margin="0,0,0,14">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding DisplayName}"
                                        Command="{Binding DataContext.SelectLanguageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding Code}"
                                        Margin="0,0,6,6">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource DlgChipButton}">
                                            <Setter Property="MinWidth" Value="70"/>
                                            <Style.Triggers>
                                                <DataTrigger Value="True">
                                                    <DataTrigger.Binding>
                                                        <MultiBinding Converter="{StaticResource StringEqualsConverter}">
                                                            <Binding Path="Code"/>
                                                            <Binding Path="DataContext.SelectedLanguage" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                        </MultiBinding>
                                                    </DataTrigger.Binding>
                                                    <Setter Property="Foreground" Value="{StaticResource DlgAccent}"/>
                                                    <Setter Property="Background" Value="{StaticResource DlgAccentSoft}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource DlgAccent}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{DynamicResource Settings.ModuleManagement}" Style="{StaticResource DlgSectionTitle}"/>
                    <ItemsControl ItemsSource="{Binding ModuleSettings}" Margin="0,0,0,14">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource DlgInputBackground}"
                                        BorderBrush="{StaticResource DlgBorder}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="8,6"
                                        Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="22"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="28"/>
                                            <ColumnDefinition Width="28"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="&#xE700;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="11"
                                                   Foreground="{StaticResource DlgTextSecondary}"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding DisplayName}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource DlgTextPrimary}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <CheckBox Grid.Column="2"
                                                  IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                                  VerticalAlignment="Center"
                                                  Margin="8,0,8,0"
                                                  ToolTip="{DynamicResource Settings.ShowHide}"/>
                                        <Button Grid.Column="3"
                                                Style="{StaticResource DlgGhostButton}"
                                                Content="↑"
                                                Width="24"
                                                Height="24"
                                                Padding="0"
                                                Margin="0,0,4,0"
                                                IsEnabled="{Binding CanMoveUp}"
                                                ToolTip="{DynamicResource Settings.MoveUp}"
                                                Command="{Binding DataContext.MoveModuleUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Grid.Column="4"
                                                Style="{StaticResource DlgGhostButton}"
                                                Content="↓"
                                                Width="24"
                                                Height="24"
                                                Padding="0"
                                                IsEnabled="{Binding CanMoveDown}"
                                                ToolTip="{DynamicResource Settings.MoveDown}"
                                                Command="{Binding DataContext.MoveModuleDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{DynamicResource Settings.QuickTextClipboard}" Style="{StaticResource DlgSectionTitle}"/>
                    <Grid Style="{StaticResource DlgToggleRow}">
                        <TextBlock Text="{DynamicResource Settings.EnableClipboardHistory}"
                                   FontSize="12"
                                   Foreground="{StaticResource DlgTextPrimary}"
                                   VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" IsChecked="{Binding ClipboardHistoryEnabled}"/>
                    </Grid>
                    <Grid Style="{StaticResource DlgToggleRow}">
                        <TextBlock Text="{DynamicResource Settings.FilterSensitive}"
                                   FontSize="12"
                                   Foreground="{StaticResource DlgTextPrimary}"
                                   VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" IsChecked="{Binding ClipboardSensitiveFilterEnabled}"/>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="{DynamicResource Settings.MaxSavedCount}"
                                   FontSize="12"
                                   Foreground="{StaticResource DlgTextPrimary}"
                                   VerticalAlignment="Center"/>
                        <WrapPanel HorizontalAlignment="Right">
                            <Button Style="{StaticResource DlgGhostButton}"
                                    Content="20"
                                    Width="44"
                                    Height="28"
                                    Padding="0"
                                    Margin="0,0,5,0"
                                    Command="{Binding SelectClipboardHistoryLimitCommand}"
                                    CommandParameter="20"/>
                            <Button Style="{StaticResource DlgGhostButton}"
                                    Content="50"
                                    Width="44"
                                    Height="28"
                                    Padding="0"
                                    Margin="0,0,5,0"
                                    Command="{Binding SelectClipboardHistoryLimitCommand}"
                                    CommandParameter="50"/>
                            <Button Style="{StaticResource DlgGhostButton}"
                                    Content="100"
                                    Width="48"
                                    Height="28"
                                    Padding="0"
                                    Margin="0,0,5,0"
                                    Command="{Binding SelectClipboardHistoryLimitCommand}"
                                    CommandParameter="100"/>
                            <Button Style="{StaticResource DlgGhostButton}"
                                    Content="200"
                                    Width="48"
                                    Height="28"
                                    Padding="0"
                                    Command="{Binding SelectClipboardHistoryLimitCommand}"
                                    CommandParameter="200"/>
                        </WrapPanel>
                    </Grid>
                    <Grid Margin="0,0,0,14">
                        <TextBlock Text="{Binding ClipboardHistoryCurrentText}"
                                   FontSize="11"
                                   Foreground="{StaticResource DlgTextSecondary}"
                                   VerticalAlignment="Center"/>
                        <Button Style="{StaticResource DlgGhostButton}"
                                Content="{DynamicResource QuickText.ClearHistory}"
                                Padding="10,0"
                                HorizontalAlignment="Right"
                                Command="{Binding ClearClipboardHistoryFromSettingsCommand}"/>
                    </Grid>

                    <TextBlock Text="{DynamicResource Settings.System}" Style="{StaticResource DlgSectionTitle}"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="{DynamicResource Settings.BackupDescription}"
                                   FontSize="12"
                                   Foreground="{StaticResource DlgTextPrimary}"
                                   VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource DlgGhostButton}"
                                    Content="{DynamicResource Settings.Backup}"
                                    Padding="12,0"
                                    Margin="0,0,8,0"
                                    Command="{Binding BackupTodosCommand}"/>
                            <Button Style="{StaticResource DlgGhostButton}"
                                    Content="{DynamicResource Settings.Restore}"
                                    Padding="12,0"
                                    Command="{Binding RestoreTodosCommand}"/>
                        </StackPanel>
                    </Grid>
                    <Grid Style="{StaticResource DlgToggleRow}">
                        <TextBlock Text="{DynamicResource Settings.Startup}"
                                   FontSize="12"
                                   Foreground="{StaticResource DlgTextPrimary}"
                                   VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" IsChecked="{Binding StartupEnabled}"/>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                        <Button Style="{StaticResource DlgGhostButton}"
                                Content="{DynamicResource Settings.ResetLayout}"
                                Padding="10,0"
                                Margin="0,0,8,0"
                                Command="{Binding ResetLayoutCommand}"/>
                        <Button Style="{StaticResource DlgGhostButton}"
                                Content="{DynamicResource Settings.ResetDefaults}"
                                Padding="10,0"
                                Command="{Binding ResetToDefaultsCommand}"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="{DynamicResource Settings.WeatherApi}" Style="{StaticResource DlgSectionTitle}" VerticalAlignment="Center"/>
                        <Button HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Padding="8,2"
                                Command="{Binding ToggleWeatherApiEditCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource DlgGhostButton}">
                                    <Setter Property="Content" Value="{DynamicResource Common.Edit}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditingWeatherApi}" Value="True">
                                            <Setter Property="Content" Value="{DynamicResource Common.Done}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                    <TextBlock Text="{DynamicResource Settings.WeatherApiHint}"
                               FontSize="10"
                               Foreground="{StaticResource DlgTextSecondary}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="58"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="API Host" Style="{StaticResource DlgLabel}"/>
                        <TextBox x:Name="WeatherApiHostTextBox"
                                 Grid.Column="1"
                                 Style="{StaticResource DlgInlineTextBox}"
                                 Text="{Binding WeatherApiHost, UpdateSourceTrigger=PropertyChanged}"
                                 IsReadOnly="{Binding IsEditingWeatherApi, Converter={StaticResource InverseBooleanConverter}}"
                                 PreviewMouseLeftButtonDown="WeatherApiTextBox_OnPreviewMouseLeftButtonDown"/>
                    </Grid>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="58"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="API Key" Style="{StaticResource DlgLabel}"/>
                        <TextBox x:Name="WeatherApiKeyTextBox"
                                 Grid.Column="1"
                                 Style="{StaticResource DlgInlineTextBox}"
                                 Text="{Binding WeatherApiKey, UpdateSourceTrigger=PropertyChanged}"
                                 IsReadOnly="{Binding IsEditingWeatherApi, Converter={StaticResource InverseBooleanConverter}}"
                                 PreviewMouseLeftButtonDown="WeatherApiTextBox_OnPreviewMouseLeftButtonDown"/>
                    </Grid>

                    <TextBlock Text="{DynamicResource Settings.About}" Style="{StaticResource DlgSectionTitle}" Margin="0,14,0,8"/>
                    <Grid Margin="0,0,0,6">
                        <TextBlock Text="{Binding CurrentVersionText}"
                                   FontSize="12"
                                   Foreground="{StaticResource DlgTextPrimary}"
                                   VerticalAlignment="Center"/>
                        <Button Style="{StaticResource DlgGhostButton}"
                                Content="{DynamicResource Settings.CheckUpdates}"
                                Padding="10,0"
                                HorizontalAlignment="Right"
                                Command="{Binding CheckForUpdatesCommand}"
                                IsEnabled="{Binding IsCheckingForUpdates, Converter={StaticResource InverseBooleanConverter}}"/>
                    </Grid>
                    <TextBlock Text="{Binding UpdateStatusMessage}"
                               FontSize="11"
                               Foreground="{StaticResource DlgTextSecondary}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,4"/>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2"
                    BorderBrush="{StaticResource DlgBorder}"
                    BorderThickness="0,1,0,0"
                    Padding="14,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource DlgGhostButton}"
                            Content="{DynamicResource Common.Cancel}"
                            Height="32"
                            MinWidth="80"
                            FontSize="13"
                            Margin="0,0,10,0"
                            Command="{Binding CancelCommand}"/>
                    <Button Style="{StaticResource DlgPrimaryButton}"
                            Content="{DynamicResource Common.Save}"
                            Command="{Binding SaveCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

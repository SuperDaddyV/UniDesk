# UniDesk v1.3.3 历史发布前审查报告（归档）

> 本文仅保留 2026-06-13 的历史审查证据，不适用于当前源码或 `v2.2.1`。`v2.1.0`／`v2.2.0` 文档同样是版本快照；当前行为以 `docs/DESIGN.md` 为准，`v2.2.1` 的最终结论与发布证据以 `docs/release-audit-2.2.1.md`、`docs/release-test-matrix-2.2.1.md`、GitHub Release 资产及对应自动门禁为准，任何历史 PASS 均需重新验证。

审查时间：2026-06-13
审查对象：本地仓库 `C:\Users\Administrator\Documents\UniDesk`，远端仓库 `https://github.com/SuperDaddyV/UniDesk`

## 1. 总体结论

结论：基本可以发布，但仍建议发布前确认一个体验问题。

当前代码可以恢复依赖、正常构建、通过测试并生成发布输出；未发现真实明文密钥泄露，也未发现阻断启动的构建错误。本轮已解决“备份还原覆盖范围不足”和“当前 main 仍使用 v1.3.2 发布版本号”的问题；待办删除缺少确认按要求暂不修改。

## 2. 当前版本和发布信息

- README 安装包示例：`UniDesk_Setup_1.3.3.exe`
- 安装脚本版本：`UniDesk.iss` 中 `MyAppVersion = 1.3.3`
- 应用文件版本：`1.3.3.0`
- GitHub 最新 Release：`v1.3.2`，附件包含 `UniDesk_Setup_1.3.2.exe`
- 当前远端 main：`96d615c Add multilingual README pages`
- `v1.3.2` 标签：`af03623 Release UniDesk 1.3.2 stability improvements`
- 版本一致性：本地 README、安装脚本和应用元数据已同步为 `1.3.3`；GitHub 最新正式 Release 仍是 `v1.3.2`，发布当前代码时应新建 `v1.3.3` 标签和 Release。

## 3. 构建结果

| 命令 | 结果 |
| --- | --- |
| `dotnet restore UniDesk.sln` | 成功 |
| `dotnet build UniDesk.sln -c Debug` | 成功，0 警告，0 错误 |
| `dotnet build UniDesk.sln -c Release` | 成功，0 警告，0 错误 |
| `dotnet test UniDesk.sln` | 成功，90 通过，0 失败 |
| `dotnet publish .\UniDesk\UniDesk.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -o publish\win-x64` | 成功 |
| `ISCC.exe .\UniDesk.iss` | 未执行，原因：本机未找到 `ISCC.exe` |
| `dotnet list UniDesk.sln package --vulnerable` | 未发现易受攻击的包 |

补充验证：当前已有 1 个 UniDesk 正在运行，启动第二个发布版实例后自动退出，进程数仍为 1，单实例保护有效。

## 4. 项目结构与架构审查

优点：
- 目录按 `Models`、`Services`、`ViewModels`、`Controls`、`Helpers`、`Resources` 拆分，整体清晰。
- 数据、天气、快捷方式、待办、便签、剪贴板、托盘、热键都有独立服务，基础边界可读。
- 测试项目覆盖数据库、设置、待办、快捷方式、便签、快捷文本、天气图标、系统指标等核心逻辑。

风险：
- `MainWindow.xaml` 约 1705 行，`MainWindow.xaml.cs` 约 743 行，`MainWindowViewModel.cs` 约 1959 行，主窗口已经偏大。
- `MainWindowViewModel` 同时负责天气、日历、硬件、快捷方式、待办、便签、快捷文本、模块配置和窗口设置，后续维护成本较高。
- `LayoutService` 仍保留旧的 `ClockWeather`、`Notes` 布局模型，当前主界面已经改为 `ModuleSettings`，属于历史兼容或未清理设计。
- 本地存在 `publish`、`installer`、`bin`、`obj` 输出目录，但 `.gitignore` 已忽略，`git ls-files` 未发现这些目录被跟踪。

## 5. 功能审查结果

| 模块 | 已确认正常 | 存在风险 | 需要人工测试 |
| --- | --- | --- | --- |
| 时间天气 | 默认密钥、用户密钥、Host、缓存、30 分钟刷新、失败兜底均有代码支持 | 自动定位兜底使用非加密 `http://ip-api.com` | 天气接口失败、无 Key、Host/Key 不匹配、不同城市 |
| 桌面日历 | 点击、上月、下月、今日、日期选择、农历显示均有代码支持 | 弹窗靠屏幕边缘的可见性需要真机看 | 多屏、DPI、靠边、折叠状态 |
| 硬件监视 | CPU、内存、GPU、温度、网速、虚拟网卡过滤均有代码支持 | 读取在 2 秒 UI 定时器中执行，少数硬件环境可能卡顿 | 无 GPU、AMD/NVIDIA/Intel、多网卡、低流量 |
| 快捷方式 | 添加、拖拽、多文件、去重、图标、排序、右键移动、数量限制均有代码支持 | 图标提取是同步操作，大量添加时可能短暂卡顿 | 拖入 exe/lnk/文件/文件夹、路径失效 |
| 待办事项 | 增删改查、完成、优先级、时间、持久化均有代码支持 | 删除待办没有确认提示 | 长标题、删除误触、备份还原 |
| 快速便签 | 多便签、自动保存、置顶、复制、删除、长文本均有代码支持 | 自动保存需要长时间编辑人工确认 | 中文、英文、表情、URL、代码片段 |
| 快捷文本 | 剪贴板监听、历史、常用短语、敏感过滤、最大数量均有代码支持 | 敏感内容过滤无法保证识别所有格式；备份会明文导出已有历史 | 剪贴板占用、重复内容、清空、收藏 |
| 模块管理 | 显示、隐藏、排序、缺失补齐、损坏恢复均有代码支持 | 隐藏模块后部分后台刷新仍保留，只是避免 UI 异常 | 全隐藏、排序、滚动 |
| 个性化设置 | 主题、透明度、宽高、字体、标题、置顶、锁定、折叠均有代码支持 | 极端背景可读性需要真机确认 | 320-520 宽、560-920 高、0.9-1.18 字体 |
| 托盘/窗口/热键 | 托盘、退出、单实例、热键注册/释放均有代码支持 | 热键冲突已提示，但错误码此前不准确，本轮已修正 | 热键修改、冲突、DPI、多屏 |

## 6. 数据库、配置、迁移和备份审查

- 数据库版本：`1.5`
- 新装建表：`Settings`、`Notes`、`Todos`、`Shortcuts`、`QuickNotes`、`ClipboardHistory`、`TextSnippets` 均有创建逻辑。
- 迁移：会补齐 `Todos.Priority`、`Shortcuts.SortOrder`、`Shortcuts.LaunchArguments`、快速便签、快捷文本表和相关设置。
- 配置：`ModuleSettings` 损坏时会恢复默认模块并补齐缺失模块。
- 备份：当前备份包含设置、模块配置、快捷方式、待办、快速便签、剪贴板历史、常用短语。
- 图标策略：快捷方式备份保存路径和排序，导入后按当前机器路径重新生成图标缓存；不直接迁移旧机器的图标缓存。
- 兼容性：旧版仅待办备份仍可导入。

## 7. 稳定性和异常处理

已确认：
- 数据库、天气、剪贴板、快捷方式、硬件读取多数异常会被捕获，不会直接崩溃。
- 全局未处理异常会写日志并避免直接退出。
- 单实例保护验证通过。

本轮已修复：
- 退出时设置保存从“异步触发后不等待”改为同步等待，降低刚改完设置就退出导致丢失的风险。
- 定位服务退出时释放网络连接。
- 热键注册失败时能拿到更准确的系统错误码。

仍需关注：
- 硬件读取集中在定时刷新中，个别机器上可能短暂卡 UI。
- 待办删除无确认，误触风险高。

## 8. 性能和资源占用

- 天气刷新频率：30 分钟，合理。
- 系统指标刷新频率：2 秒，适合轻量监控，但硬件读取来源较多，需要真机长期观察。
- 剪贴板监听：使用 Windows 消息机制，不是高频轮询。
- 快捷方式图标提取：同步执行，少量没问题，大量拖入可能短暂卡顿。
- 数据库：多数读写异步；设置保存有防抖，本轮已改善退出保存。
- 资源释放：托盘、热键、剪贴板监听、天气客户端、系统指标服务基本有释放路径。

## 9. UI / UX 审查

已确认：
- 主面板有滚动设计，模块增多不会依赖压缩硬件监视模块。
- 主题、透明度、宽高、字体大小有设置入口。
- 空状态、复制、清空、备份、还原等有反馈。
- README 图片尺寸统一为 1672x941，路径存在。

风险：
- 主窗口 XAML 体量大，后续 UI 改动容易互相影响。
- 待办删除没有确认。
- 日历弹窗、多屏/DPI、浅色桌面背景可读性需要人工看实际效果。

## 10. 隐私与安全审查

已确认：
- 剪贴板敏感过滤默认开启。
- 过滤规则覆盖 4-8 位数字、`password`、`passwd`、`token`、`api_key`、`apikey`、`secret`、`authorization`、`bearer`、`cookie`、`session`、JWT、身份证号、银行卡号。
- 被过滤内容不入库，日志只记录“已过滤”，不写入具体剪贴板内容。
- 未发现真实明文 token、secret、个人账号或本地绝对路径泄露。
- `secrets.json` 被 `.gitignore` 忽略。

本轮已修复：
- 移除了项目文件和安装脚本中“本地存在 `secrets.json` 就复制/打包”的规则。
- 重新发布后确认 `publish\win-x64` 中没有 `secrets.json`。

仍需关注：
- 自动定位兜底使用 `http://ip-api.com`，不是加密连接；建议改成 HTTPS 可用服务或在隐私说明中明确提示。
- 剪贴板历史和备份文件都可能包含用户主动保留的普通文本，备份导出后应提醒用户妥善保存。

## 11. 多语言 README 和文档审查

已确认：
- `README.md` 为英文默认页。
- `README.zh-CN.md`、`README.ja-JP.md`、`README.es-ES.md` 存在。
- 四个 README 顶部语言切换链接互通。
- 图片路径均为 `images/unidesk-*.png`，本地文件存在，GitHub raw 图片可访问。
- README 版本和安装包名称均为 `1.3.3`。
- 文档没有不合理承诺“绝对安全”。
- LumiDesk 致谢保留合理。

风险：
- `docs/DEVELOPMENT_PLAN.md` 仍有大量未完成清单和旧设计文字，建议不要把它作为正式主页材料引用。
- `docs/release-unidesk.md` 仍提到旧安装包 `UniDesk_Setup_1.1.1.exe`，如果这是历史记录可以保留；如果作为当前发布说明，应更新。

## 12. 安装包和发布配置审查

已确认：
- `AppName = UniDesk`
- `AppVersion = 1.3.3`
- `OutputBaseFilename = UniDesk_Setup_1.3.3`
- `AppId` 和 `AppMutex` 已配置。
- 默认安装目录为 `{autopf}\UniDesk`。
- 开机自启和桌面图标为可选任务。
- 卸载默认保留 LocalAppData 用户数据。
- 旧 `LumiDesk`、`VsirDesk` 任务清理逻辑用于兼容迁移，保留合理。

本轮已修复：
- 安装脚本不再打包 `UniDesk\secrets.json`。

未完成：
- 本机未安装或未找到 Inno Setup 的 `ISCC.exe`，无法实际编译安装包。
- `installer` 本地目录有旧版本安装包，但未被 Git 跟踪；发布时应只上传目标版本。

## 13. 旧名称和历史兼容审查

搜索结果判断：
- `VsirDesk` / `LumiDesk` 出现在启动项清理、旧数据迁移、旧密钥兼容解密、安装脚本卸载清理、README 致谢中，属于合理兼容或致谢。
- README、安装包显示名、主标题未发现 `VsirDesk` 误残留。
- `TODO` / `FIXME`：源码未发现发布阻断项；`docs/DEVELOPMENT_PLAN.md` 有计划清单，属于文档历史。
- `Debug.WriteLine` / `Console.WriteLine`：未发现源码调试输出。
- `localhost`：只出现在第三方 QWeather 图标包 README 中，不影响应用。
- `password` / `token` / `secret` / `api_key`：主要为敏感过滤关键词、说明文档或可选 secrets 读取逻辑，未发现真实泄露。
- `bin` / `obj` / `publish` / `installer`：本地存在，`.gitignore` 已忽略，未被 Git 跟踪。

## 14. 发布前必须修复的问题

### Critical

无。

### Major

1. 待办删除缺少确认
影响范围：用户误点或误滑可能直接删除任务。
涉及文件：`UniDesk/ViewModels/MainWindowViewModel.cs`、`UniDesk/MainWindow.xaml`、`UniDesk/Controls/TodoSwipeRow.xaml.cs`。
建议修复：删除待办前弹出确认，或提供撤销提示。

### Minor

1. 自动定位兜底使用非加密 HTTP
影响范围：自动定位失败兜底时请求第三方接口。
涉及文件：`UniDesk/Helpers/LocationProvider.cs`。
建议修复：改用 HTTPS 可用服务，或在隐私说明中注明。

2. 主窗口文件过大
影响范围：后续维护和 UI 修改成本。
涉及文件：`UniDesk/MainWindow.xaml`、`UniDesk/MainWindow.xaml.cs`、`UniDesk/ViewModels/MainWindowViewModel.cs`。
建议修复：后续按模块拆分 UserControl 和 ViewModel。

3. 硬件读取可能在少数机器上造成短暂卡顿
影响范围：GPU/温度读取异常或驱动慢的机器。
涉及文件：`UniDesk/Services/SystemMetricsService.cs`、`UniDesk/ViewModels/MainWindowViewModel.cs`。
建议修复：把较慢硬件读取放到后台任务，并限制超时。

## 15. 可后续优化的问题

- 将天气、快捷方式、待办、便签、快捷文本拆成独立模块视图和模块模型。
- 为备份文件增加格式版本说明、导入预览和导入后摘要。
- 为快捷方式批量拖入增加进度提示。
- 为剪贴板历史备份增加“是否包含剪贴板历史”的用户选择。
- 为发布流程增加脚本：清理旧输出、发布、编译安装包、计算校验值、生成 Release Notes。

## 16. 人工测试清单

| # | 测试步骤 | 预期结果 | 风险点 |
| --- | --- | --- | --- |
| 1 | 删除或备份本地 UniDesk 数据后首次启动 | 自动创建数据目录和数据库，主窗口正常显示 | 首次初始化失败 |
| 2 | 使用旧版本数据库启动 | 自动迁移并保留旧数据 | 字段缺失 |
| 3 | 已运行时再次启动 UniDesk | 第二个实例提示或退出，只保留一个进程 | 多实例写库 |
| 4 | 双击托盘、菜单显示/隐藏、设置、退出 | 行为符合预期 | 托盘残留 |
| 5 | 按默认 Ctrl+Alt+Space | 窗口显示/隐藏 | 热键冲突 |
| 6 | 修改热键后保存并重试 | 新热键生效，旧热键释放 | 释放失败 |
| 7 | 切换置顶、锁定、折叠、拖动窗口 | 状态正确保存 | 锁定仍可拖动 |
| 8 | 调整宽度 320/520 | 布局不溢出 | 文本挤压 |
| 9 | 调整高度 560/920 | 模块滚动正常 | 硬件模块被压缩 |
| 10 | 调整字体 0.9/1.18 | 文本清晰且不重叠 | 长文本撑破 |
| 11 | 切换主题和透明度 | 浅色/深色背景可读 | 低透明不可读 |
| 12 | 正常网络下查看天气 | 城市、温度、空气、湿度显示 | API 解析失败 |
| 13 | 故意填错天气 Host/Key | 显示友好失败，不崩溃 | 错误提示不清 |
| 14 | 点击时间打开日历 | 日历弹出并显示农历 | 弹窗位置 |
| 15 | 日历切换上月/下月/今日 | 日期和农历更新 | 状态不同步 |
| 16 | 无 GPU 或禁用显卡监控环境启动 | GPU 显示 `--`，不崩溃 | 驱动异常 |
| 17 | 观察上传/下载速度 | 低流量显示合理 | 虚拟网卡误计 |
| 18 | 隐藏/显示模块 | 主界面实时更新 | 空状态异常 |
| 19 | 调整模块排序 | 主面板顺序稳定 | 排序不保存 |
| 20 | 开启所有模块并滚动 | 所有模块可见 | 滚动卡顿 |
| 21 | 手动添加快捷方式 | 新项排在最后，可打开 | 路径处理 |
| 22 | 拖入 exe/lnk/文件/文件夹 | 成功添加，重复项提示 | 多文件冲突 |
| 23 | 快捷方式内部拖拽排序 | 排序保存，重启仍正确 | SortOrder 错乱 |
| 24 | 删除或改名目标路径后点击快捷方式 | 有友好失败，不崩溃 | 无提示 |
| 25 | 待办增删改查、完成/取消 | 状态和优先级正确保存 | 删除无确认 |
| 26 | 快速便签输入长文本并等待自动保存 | 关闭重开后内容保留 | 自动保存丢失 |
| 27 | 便签置顶、复制、删除 | 排序和反馈正确 | 删除误触 |
| 28 | 复制普通文本 | 剪贴板历史新增 | 监听失败 |
| 29 | 复制验证码、密码、Token、Cookie | 默认不进入历史 | 过滤漏判 |
| 30 | 新增、编辑、删除、复制常用短语 | 数据保存并可一键复制 | 排序错乱 |
| 31 | 清空剪贴板历史 | 有确认，清空后为空 | 误清空 |
| 32 | 执行备份 | 生成 JSON 文件并提示成功 | 文件权限 |
| 33 | 执行还原 | 已包含数据被覆盖并刷新 | 当前备份范围不足 |
| 34 | 恢复默认布局 | 模块恢复默认顺序 | 设置未保存 |
| 35 | 恢复默认设置 | 外观和开关回默认 | 用户配置丢失 |
| 36 | 安装 `UniDesk_Setup_1.3.3.exe` | 安装后可启动 | 安装脚本未实编 |
| 37 | 覆盖安装升级 | 用户数据保留 | 数据迁移 |
| 38 | 卸载 | 程序文件移除，本地用户数据默认保留 | 误删数据 |
| 39 | 点击四个 README 语言链接 | 都能互相跳转 | 链接错误 |
| 40 | 在 GitHub 查看 README 图片 | 图片正常显示 | 图片路径错误 |

## 17. 最终建议

- 是否建议发布当前版本：基本可以作为 `1.3.3` 发布；待办删除确认按当前产品取舍保留。
- 是否建议打 Git tag：建议打新标签 `v1.3.3`。
- 是否建议新建 GitHub Release：建议在 `v1.3.3` 标签上新建 Release，并只上传 `UniDesk_Setup_1.3.3.exe`。
- 是否需要更新 README / CHANGELOG：README 版本一致；建议新增或更新 CHANGELOG/Release Notes，说明本轮安全和稳定性修正。
- 是否需要新增 Issues：建议为“主窗口模块拆分”“HTTP 定位兜底”“硬件读取后台化”建立跟踪项。

## 本轮已直接修复

- 移除 `secrets.json` 复制和打包规则。
- 修正退出时设置保存可能未完成的问题。
- 释放定位服务的网络连接。
- 修正热键注册失败错误码读取。
- 删除未被引用且内容无效的测试天气图片资源。
- 扩展备份还原范围，新增设置、模块配置和快捷方式。
- 将应用、安装脚本、README 和发布说明更新为 `1.3.3`。

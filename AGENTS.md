# UniDesk 项目规范

## 项目边界

- 目标平台为 Windows 10 1903+ 与 Windows 11，目标框架为 `net9.0-windows10.0.18362.0`。
- UI 使用 WPF；数据使用 `Microsoft.Data.Sqlite`；不得引入第二套 UI、数据库或依赖注入框架。
- 默认中文沟通；代码、命令、类型、成员和文件名使用英文。

## 修改纪律

- 行为或数据语义变更前，先更新 `docs/DESIGN.md` 或对应设计文档，再修改测试和生产代码。
- 只修改当前任务直接涉及的文件；稳定性修复不得夹带 UI 重构、模块拆分、新功能或格式化。
- 新依赖必须先证明标准库和现有依赖无法满足；本轮稳定性修复不增加 NuGet 包。
- 数据库 schema、历史数据迁移、CI/CD、密钥、自动删除文件和公开发布必须单独获得用户确认。

## 数据安全

- 备份恢复必须在写入前完成全量语义校验。
- 恢复中的数据库变更必须使用单连接、单事务；任一步失败必须回滚全部设置和业务数据。
- 持久化失败不得伪装成成功；返回计数必须来自实际受影响行数。
- 数据库初始化失败必须记录日志并向上抛出，应用不得带病启动。

## 并发与性能

- Service 不得假定普通集合可被多线程安全读写；共享可变状态必须有明确锁或线程限定。
- `Microsoft.Data.Sqlite` 的异步 API 实际同步执行，不得据此假定数据库工作已离开 UI 线程。
- WAL 可用于改善读写并发；不得启用 `Cache=Shared`，不得擅自缩短默认 30 秒锁等待。

## 测试与验证

- 功能和缺陷修复使用 TDD：先写失败测试并确认失败原因，再写最小实现。
- 首选真实临时 SQLite 数据库；仅在必须注入持久化故障时使用完整、行为明确的测试替身。
- 局部验证后必须运行：`dotnet test UniDesk.sln -c Release --no-restore`。
- 不得通过跳过、屏蔽或弱化测试使构建变绿。
